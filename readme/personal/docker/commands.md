# docker | commands

*Project folder must contain Dockerfile and docker-compose.yml*

### Start 

```bash
docker-compose up
```

### Build

```bash
docker-compose build
```

### Start and Build

```bash
docker-compose up --build
```

### Down

```bash
docker-compose down
```

### Clean cache

```bash
docker builder prune
```